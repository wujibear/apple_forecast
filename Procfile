# Thanx Rewards Development Procfile
# Foreman will start both services concurrently

# Rails API server
api: (cd thanx_api && bundle exec --gemfile Gemfile rails server -p 3000)

# React development server
client: (cd thanx_client && npm run dev)