name: Client tests

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'apple_client/**'
  pull_request:
    paths:
      - 'apple_client/**'

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: apple_client/package-lock.json
    
    - name: Install dependencies
      run: |
        cd apple_client
        npm ci

    - name: Check code formatting
      run: |
        cd apple_client
        npm run format:check
    
    - name: Run type check
      run: |
        cd apple_client
        npm run typecheck
    
    - name: Run build test
      run: |
        cd apple_client
        npm run build

    - name: Run tests
      run: |
        cd apple_client
        npm run test:run

    - name: Check coverage
      run: |
        cd apple_client
        npm run test:coverage 